vector<int> solution(vector<int> a) {
 int n=a.size();
 vector<int>Result(2,0);
 for (int i=0;i<n;i++){
     
     if (i%2==0){
         Result[0]+=a[i];
     }else {
         Result[1]+=a[i];
     }
     
 }
 return Result;
}
